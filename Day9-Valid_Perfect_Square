class Solution:
    def isPerfectSquare(self, num: int) -> bool:
        i = 1
        while True:
            if i*i==num:
                return True
            if i*i>num:
                return False
            i+=1
        #O(logn) time
